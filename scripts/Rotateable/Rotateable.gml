function rotateable_change_dir(_dir)
{
	/*
	with (pRotateable)
	{
		var _angle_width = rot_limit_start * 2;
		var _upper, _lower;
		
		if (_dir == DIR.U || _dir == DIR.D)
		{
			_upper = rot_limit_start;
		}
		else
		{
			_upper = rot_limit_start + 90;
		}
		
		rot_limit = _upper;
		_lower = _upper - _angle_width;
		
		add_to_debug_map("_lower: " + string(_lower));
		add_to_debug_map("_angle_width: " + string(_angle_width));
		add_to_debug_map("_upper: " + string(_upper));
		
		physics_joint_set_value(joint, phy_joint_upper_angle_limit, _upper);
		physics_joint_set_value(joint, phy_joint_lower_angle_limit, _lower);
	}
	*/
}